version: '3.8'

services:
  api:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - mongodb
      - redis
    networks:
      - cognivexa-network

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - cognivexa-network

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - cognivexa-network

networks:
  cognivexa-network:
    driver: bridge

volumes:
  mongodb_data:
